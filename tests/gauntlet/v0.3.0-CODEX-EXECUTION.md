# v0.3.0 gauntlet — independent non-Anthropic execution pass

The v0.3.0 gauntlet (`tests/gauntlet/v0.3.0-battery.js`) was executed by an **independent, non-Anthropic harness** — Codex (OpenAI), invoked outside the Anthropic orchestrator — to confirm the result is not an artifact of the build environment. Codex was instructed only to run the committed command and report verbatim output; it modified no files.

**Command:** `node tests/gauntlet/v0.3.0-battery.js`

**Codex result (verbatim):**
```
  TOTAL scenarios: 282
  HOLD:            282  (component behaved correctly)
  adjudicated FP:  10  (documented tester false-positives; component is correct — see ADJUDICATION.md)
  BREAK:           0  (unexpected component failures)

RESULT: 282 scenarios, all HOLD, 0 BREAK (>=180 target met)
```
**Exit code:** `0`

This mirrors the orchestrator run exactly (282 / all HOLD / 0 BREAK). The battery itself aggregates suites authored by four non-Anthropic families (Codex/GPT, DeepSeek, Mistral, Qwen); this pass confirms a non-Anthropic *harness* reproduces the aggregate. Anyone can reproduce it from the committed script — the battery is deterministic (no clock/random in the decision paths).
